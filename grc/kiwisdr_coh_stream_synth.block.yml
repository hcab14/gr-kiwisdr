# auto-generated by grc.converter

id: kiwisdr_coh_stream_synth
label: coh_stream_synth
category: '[kiwisdr]'

parameters:
-   id: fs_in
    label: fs_in
    dtype: int
    default: '12000'
-   id: df_in
    label: df_in
    dtype: int
    default: '8000'
-   id: num_streams
    label: num_streams
    dtype: int
    default: '3'
    hide: part

inputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ num_streams }

outputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
-   label: resamp
    domain: stream
    dtype: complex
    multiplicity: ${ num_streams }
    optional: true
-   label: phase_qa
    domain: stream
    dtype: complex
    multiplicity: ${ num_streams-1 }
    optional: true

templates:
    imports: import kiwisdr
    make: kiwisdr.coh_stream_synth(${num_streams}, ${fs_in}, ${df_in})

file_format: 1
