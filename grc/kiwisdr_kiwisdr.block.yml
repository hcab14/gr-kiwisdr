# auto-generated by grc.converter

id: kiwisdr_kiwisdr
label: kiwisdr
category: '[kiwisdr]'

parameters:
-   id: host
    label: Host name
    dtype: string
-   id: port
    label: Port number
    dtype: string
-   id: mod
    label: Modulation
    dtype: string
    default: iq
    options: [iq]
    option_labels: [IQ]
-   id: low_cut_Hz
    label: IF filter low cut (Hz)
    dtype: int
-   id: high_cut_Hz
    label: IF filter high cut (Hz)
    dtype: int
-   id: rx_freq_kHz
    label: RX center frequency (kHz)
    dtype: float

outputs:
-   label: IQ
    domain: stream
    dtype: complex

templates:
    imports: import kiwisdr
    make: kiwisdr.kiwisdr(${host}, ${port},${rx_freq_kHz},${low_cut_Hz},${high_cut_Hz})
    callbacks:
    - set_rx_parameters(${rx_freq_kHz},${low_cut_Hz},${high_cut_Hz})

file_format: 1
