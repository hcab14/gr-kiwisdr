# auto-generated by grc.converter

id: kiwisdr_align_streams
label: align_streams
category: '[kiwisdr]'

parameters:
-   id: num_streams
    label: num_streams
    dtype: int
    default: '3'
    hide: part
-   id: same_kiwi
    label: same_kiwi
    dtype: bool
    default: 'True'

inputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ num_streams }

outputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ num_streams }
asserts:
- ${ num_streams >= 2 }

templates:
    imports: import kiwisdr
    make: kiwisdr.align_streams(${num_streams}, ${same_kiwi})

file_format: 1
